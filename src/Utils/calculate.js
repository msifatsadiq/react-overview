const add = (first, second) => {
    return first + second;
}

const multi = (first, second) => {
    return first * second;
}

const divideThePastNumberByTheSecondNumber = (first, second) => {
    return first / second;
}

// export default add
export { add, multi, divideThePastNumberByTheSecondNumber }